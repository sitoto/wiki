%title 首页
%toc 数据录入平台 

<hr>
= 采集 - 定向采集规范 =
[[CaiJi|采集说明]] 

= 数据源 - 数据录入帮助 =
[[Jiapei|数据导入系统]]

= 开发知识点 =
[[rubyonrails|Rails & 数据库 etc]]

= 要点记录 =
[[knowledge]]
<hr>

== 2014-04-09 ==
* excel 导入 数字问题
* 导入全部存为文本格式

== 2014-03-28 ==
* 增加parts 的 position；必须增加，因为position也有意义, 重复part也有意义；
* 去除重复方法：（realcar_no, main_id, sub_no, part_position）唯一

== 2014-03-27 == 
* 增加Epctruckmodels index show
* 单表结构 修改 主从结构;
* 车型表(epctruckmodels) + 明细大表(epctrucks);


== 2014-03-26 == 
* 增加Epctrucks index show


==[[2014-03-25]]==
* 全部日志记录
* 增加采集项目管理（自动采集管理）
* 类目里面新增TYPE，
  * 手动：手动增加标准属性
  * 系统预定义：标准属性预先定义好 

==2014-03-21==
* 增加对excel的导入支持；
* 原来的csv方式继续有效；

==[[2014-03-11]]==
* 在数据录入平台中加入 采集模块
* fetch/AAP

==2014-02-13==
* 修改图片存储thumb(50*50),middle(800*800)
* 增加图片关联-类目关联(has_many)和数据项关联(has_and_belongs_to_many)

==2014-01-22==
* 未导入的 文件 可以删除，已经导入的不能删除；
* 文件被删除的时候，导入到临时库的数据同时也被删除；
* 导入记录 里 增加 删除记录？

==2014-01-17==
* 关联类目设计？
* 图片尺寸动态设计？


==2014-01-17==
* 动态权限设置，部署到服务器2.9上
* 图片上传和图片管理

==2014-1-10==
* 新增图片存储
 * 两种尺寸
* 增加动态权限控制
 * 设置权限管理

==2014-1-6==
* 增加图片存储？,设置图片 多尺寸、
* 车型图片对应
* 如何做数据关联？？？
